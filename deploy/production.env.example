DOMAIN=example.com
# The app is reached by Caddy over the private Compose network.
HOST=0.0.0.0
TRUSTED_PROXY_CIDRS=
LOG_FORMAT=json
METRICS_HOST=0.0.0.0
METRICS_PORT=9090
# Set this to the exact public frontend origin(s), comma-separated.
CORS_ALLOWED_ORIGINS=https://contracter-1t9.pages.dev
