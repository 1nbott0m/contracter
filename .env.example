# Copy to an ignored local file only when needed. Never commit real credentials.
TEST_DATABASE_URL=postgres://USER@HOST:PORT/defaultdb?sslmode=require

# Prefer a hidden terminal prompt instead of writing the password to a file:
# read -s "PGPASSWORD?PostgreSQL password: "; export PGPASSWORD; echo
